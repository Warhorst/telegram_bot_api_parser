{{#each dto.fields}}
use super::{{this.type_snake_case}}::{{this.type}};\n
{{/each}}

#[derive()]
pub struct {{dto.name}} {
    {{#each dto.fields}}
    {{this.name}}: {{this.type}},\n
    {{/each}}
}

impl {{dto.name}} {
    {{#each dto.fields}}
    pub fn get_{{this.name}}(&self) -> &{{this.type}} { &self.{{this.name}} }\n\n
    {{/each}}
}